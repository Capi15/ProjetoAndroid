<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="intro.multiecras.flowerpower">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".InCategoria"
            android:label="@string/title_activity_in_categoria"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Atividades.AtivityDadosConta"
            android:label="@string/title_activity_ativity_dados_conta"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CategoriaActivity" />
        <activity
            android:name=".Atividades.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>